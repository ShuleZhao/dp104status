# Ticktype DP104 控制协议

在真机上验证过的逆向结果。设备内部代号 **EVO 104**，固件厂商标识 `Tab`。

来源：`cfg.ticktype.com`（VIA 官方 web app 的自托管分支）的 JS bundle，
配合 macOS 上的实机读写验证。

---

## 1. 两条通道

DP104 同时暴露两个接口，**必须都用**：

| 通道 | 地址 | 负责 | 分片 |
| --- | --- | --- | --- |
| QMK Raw HID | `e560:e104`，usagePage `0xFF60` / usage `0x61` | 灯光、屏幕模式、屏幕文本 | 25 字节 |
| USB CDC 串口 | `/dev/cu.usbmodem*` @ 115200 8N1 | 屏幕像素帧数据 | 56 字节 |

> **坑**：固件对 HID 的 `0xD1`（TAB_BLOCKS）系列命令一律回 `0xff` = 不支持。
> bundle 里那套 HID 批量上传代码在这块键盘上是死代码，VIA 靠配置里的
> `"cdc": true` 走了串口分支。不实测发现不了。
>
> 串口名里的数字是 USB 拓扑位置（`usbmodem214304` ← 设备在 `@02143000`），
> **换个 USB 口就会变**，必须按 VID/PID 枚举，不能硬编码。

## 2. 设备参数

来自 `https://cfg.ticktype.com/tabkb/configs.json`：

```json
{ "name": "EVO 104", "vendorId": "0xE560", "productId": "0xE104", "cdc": true,
  "matrixLighting": { "rows": 8, "cols": 24, "cdc": true,
                      "texts": { "count": 5, "length": 30, "default": "TICKTYPE" },
                      "info": true } }
```

- 点阵屏 **8 行 × 24 列 = 192 像素**，全彩逐像素可寻址
- 文本槽 **5 个 × 30 字符**
- VIA 协议版本 12；CDC 能力查询（HID `0xBF`）返回"不支持"，但配置文件声明 `cdc: true`

完整 VIA definition：`https://cfg.ticktype.com/definitions/v3/3848331524.json`
（id = `vendorId * 65536 + productId`）

## 3. Raw HID

报文 33 字节：`[0x00 报告ID, cmd, ...args]`，即 32 字节有效载荷。响应回显 `[cmd, ...args]`。

```
0x07  CUSTOM_MENU_SET_VALUE   只改 RAM，立即生效
0x08  CUSTOM_MENU_GET_VALUE
0x09  CUSTOM_MENU_SAVE        写 EEPROM ── 状态灯场景永远不要发这条
```

**响应首字节是 `0xff` 表示固件不支持该命令**（不是错误码在末尾——早期误判过）。

### 通道与取值

| 通道 | 名称 | value id |
| --- | --- | --- |
| **22** (0x16) | 逐键 RGB 背光 | 1=亮度(0-255) 2=效果(0-44) 3=速度 4=颜色 |
| **21** (0x15) | 底部氛围灯 | 1=亮度 2=效果(0-42) 3=速度 4=颜色 |
| **26** (0x1A) | 点阵屏 | 1=亮度 2=模式 4=颜色 **5=文本槽** 6=info 内容 |

**颜色是 HSV 不是 RGB**：`0x07 [channel, 4, hue, sat]`，两字节各 0-255，明度由 value 1 单独控制。

屏幕模式（channel 26 / value 2）：

```
0=OFF  1=TYPE(实时打字)  2=CUSTOM(自定义帧)  3=INFO(系统状态)
4=SPARK(烟花)  5=AUDIO(音频响应)  6=SCROLL(滚动文字)
```

### 文本槽（状态显示首选）

```
读  0x08 [26, 5, slot, offset, min(30-offset, 26)]   → 响应第 6 字节起是 ASCII
写  0x07 [26, 5, slot, offset, len, ...chars]
```

slot 0-4，每槽 30 字符。写入时**补 NUL 到 30**，否则旧内容的尾巴会残留。

已验证：SCROLL 模式下改写文本槽，屏幕**立即更新**，无需任何刷新动作。

## 4. CDC 串口

包格式极简：**定长 64 字节 `[cmd, ...args, 0 填充]`**，无包头、无校验和。
响应也是 64 字节，`resp[1:]` 回显 args。

```
0xC0  矩阵帧头   [frames, fps, rows, cols]
                 resp[5]==0xEE 表示错误；resp[6] 表示数据分片是否需要等 ACK
0xC1  矩阵数据   [offset(4字节大端), len, ...payload]   分片 56 字节
0xE0  文件头     [filename 20 字节]
0xE1  文件数据   同 0xC1 格式
0xE2  中止
0xEF  固件查询
0xF0  固件信息
0xF1  固件数据
```

### 像素载荷格式

**每像素 3 字节 HSV**（不是 RGB —— VIA 里叫 `get256HSV`）：

```
H = round(255 * hue / 360)      hue 单位是度
S = round(255 * saturation)
V = round(255 * value)
```

排列顺序：**帧 → 行 → 列**，即 `data[frame][row][col]`。
单帧 = 8 × 24 × 3 = **576 字节**，分 11 片发送。

导出到文件时用的是 RGB + 32 字节文件头，**跟上传给设备的格式不同**，别搞混。

## 5. 刷新语义

| 操作 | 是否实时 |
| --- | --- |
| HID 改亮度 / 模式 / 颜色 | ✅ 立即 |
| HID 改文本槽（SCROLL 模式下） | ✅ 立即 |
| CDC 上传 CUSTOM 帧 | ✅ 立即，**但绝不能之后再切模式** |

**上传即生效，不要碰模式。** 实测（全红 / 全绿 / 全蓝 三段对照）：
纯上传那段颜色变了；上传后做 `CUSTOM→OFF→CUSTOM` 或绕第三个模式再回来的两段都**没变**
——因为重新进入 CUSTOM 会让固件加载它自己存的那一帧，把刚上传的覆盖掉。

唯一需要设模式的时机是**第一次接管**：屏幕从没有过有效 CUSTOM 帧时，
要先进一次模式初始化，之后只上传。

VIA 官方流程里没有任何 commit/refresh 命令（`0xE2` 是 abort），这与"上传即生效"一致。

## 5.1 ⚠️ CDC 端点会失效，原因未知

**现象**：设备对所有 CDC 命令一律无响应——连最简单的 `0xEF` 固件查询也不回，
读到 0 字节。**只有物理拔插 USB 能恢复**，软件无法自救。
HID 通道完全不受影响，期间仍能正常读写。

观察到两次，都是在 daemon 运行数十分钟之后。

**触发条件尚未查明。** 以下六个假设都做过针对性实验，全部否定：

| 假设 | 实验 | 结果 |
| --- | --- | --- |
| 传输层本身脆弱 | 单连接连发 60 帧，间隔 0.5s | 全部成功 |
| HID 读取与 CDC 交叉 | 每帧前读一次屏幕模式，40 帧 | 全部成功 |
| HID 写入与 CDC 交叉 | 每帧前写一次屏幕模式，20 帧 | 全部成功 |
| 反复开关串口 | 每帧都 open/close，30 次 | 全部成功 |
| 连接空闲超时 | 帧间隔从 1s 递增到 20s | 全部成功 |
| 传输被中途打断 | 发头 + 3/11 分片后停止，再发完整帧 | 能自行恢复 |

累计约 180 次上传，未能复现。

**尚未排除**：分钟级的长空闲（键盘自带休眠设置，默认 5 分钟）、
系统休眠 / USB 挂起、以及某些尚未想到的组合。

**本项目采取的对策不是预防，而是优雅降级**：pixel 帧发送失败即切换到
HID 文本显示（该通道从未出过问题），并每 30 秒重试 pixel。
串口开一次后全程持有——这是出于谨慎，不是因为已经证明开关有害。

> 早期版本的本文件断言"反复开关串口会弄死端点"。那是从第一次事故的日志推断的，
> 但日志里重复的重连是失败的**后果**而非原因。上表第四行的实验否定了它。
> 保留这段记录，是为了避免后来者重复同一个错误推断。

## 6. 相关自定义键码

`TJM_MOD` = "MATRIX LED Mode"，即键盘上手动切换屏幕模式的那个键。
另有 `TJM_RMOD`（反向）、`LNT_MOD`、`TRGB_*` 系列。

## 7. 对状态灯项目的结论

**推荐：SCROLL + 文本槽走 HID 单通道。** 实时、简单、不碰 flash，
30 字符足够写 `CODEX·WORK CLAUDE·WAIT`。

CUSTOM 像素帧适合做低频更新的图形（比如每分钟刷一次的概览），
不适合逐事件的实时状态——每次刷新都要模式往返，视觉上会闪。

背光（channel 22）用 `0x07` 写颜色，**永远不发 `0x09`**，避免 flash 磨损。
接管前先读出用户原有的 `[22,1..4]` 存好，退出时还原。
